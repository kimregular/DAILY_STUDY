#ios 

```swift
import SwiftUI

struct ContentView: View {
	var body: some View {
		
		VStack {
			Image(systemName: "globe")
			.imageScale(.large)
			.foregraoundStyle(.tint)
			Text("Hello, world")
		}
	}
}
```

xcode에서 처음 생성한 ios 프로젝트에는 이런 모습을 한 코드가 있다. 여기서 VStack을 알아보자.

## VStack

VStack은 대표적인 `컨테이너 뷰`이다. 컨테이너 뷰 안에는 여러가지 뷰를 담고, 정렬할 수 있다.

```swift
struct ContentView: View {

    var body: some View {

        VStack {

            Rectangle()
                .frame(width: 50, height: 50)
                .foregroundStyle(.teal)

            HStack{
                Rectangle()
                    .frame(width: 100, height: 100)
                    .foregroundStyle(.red)

                Capsule()
                    .frame(width: 200, height: 50)
            }

            Ellipse()
                .frame(width: 50, height: 50)
                .foregroundStyle(.blue)

            Spacer()

            Color.green
                .frame(width: 100, height: 50)
        }
    }
}
```

![[Pasted image 20260407221250.png]]

코드를 렌더링하면 그림과 같은 모습이 출력된다. 

V -> Vertical
H -> Horizontal

## ZStack
```swift
struct ContentView: View {
    var body: some View {
        
        ZStack {
            Circle()
                .foregroundStyle(.orange)
                .frame(width: 200, height: 200, )
            
            Rectangle()
                .foregroundStyle(.teal)
                .frame(width: 180, height: 60)
            
            Rectangle()
                .frame(width: 60, height: 180)
                .foregroundStyle(.indigo)
            
            Circle()
                .foregroundStyle(.pink)
                .frame(width: 100, height: 100)
            
            Circle()
                .foregroundStyle(.white)
                .frame(width: 40, height: 40)
                
        }
    }
}
```

ZStack은 뷰들을 겹쳐서 배열하는 컨테이너 뷰이다. 코드 상단에 있는 뷰가 하단, 아래에 있는 뷰가 상단에 위치한다.

![[Pasted image 20260407222114.png]]